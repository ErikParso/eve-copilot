# Workspace Copilot Instructions

- This project is a React + TypeScript + MUI app for EVE Online courier contract filtering.
- Use the existing project scaffold and avoid adding unrelated files.
- Keep implementation focused on the contract helper page and the requested filter/table UX.
