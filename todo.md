# todo list

the completed contract is still in table even when i accpeted it 20 minutes before. It takes too long to actualize that.

==========================

make advanced attractivity index calculation that will replace hardcoded methods. User can select the weights for multiple factors:

* total income - higher the better,
* isk per jump - higher the better,
* danger index - lower the better,
* total jumps - lower the better,
* colateral - lower the better,
* cargo weight - lower better,
* jumps to pickup location - lower the better, contracts with closer pickup location will be less likely taken by other players,
* time active - higher better, older contracts will be less likely taken by other players while i come to pickup,
* any other factors i missed ??? suggested factors makes sense ?

make few basic presets name them and describe what they are intended for (user can click pre set to apply weights).
the weights can be set in a modal window (a lot of controls to be in main panel) using sliders or by clicking on preset that automatically adjusts those values.
make the weights from 0 to 10, use markers on slider, when 0 the factor is not applied.

tooltip with detailed information about calculation and specific numbers stays

===========================

the value isk per jump takes in account also jumps to pickup ? Investigate.

===========================

more shades for router squares use exact palette as eve has in game.
